#include "structs.h"

//everything we want to use as an extern elsewhere in other files goes here

Game game;
Control input;
Star stars[MAX_STARS];
Entity player, *self, entity[MAX_ENTITIES];
Sprite sprite[MAX_SPRITES];
Animation animation[MAX_ANIMATIONS];
